/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    // Aquí puedes añadir otras opciones experimentales válidas si las necesitas
  },
  // Aquí puedes añadir otras configuraciones válidas
};

export default nextConfig;
